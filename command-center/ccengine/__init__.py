"""Command Center engine — core package (CLI-first).

The CLI (`cc.py`) is a thin face over this package so a future local API can wrap
the identical core. Phase 0 ships the skeleton + the read-only `status` command.
"""

__version__ = "0.0.2-phase1"
